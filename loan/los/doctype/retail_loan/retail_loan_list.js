frappe.listview_settings['Retail Loan'] = {
	refresh: function(listview) {
        $('button.btn-reset.sidebar-toggle-btn').hide();
        $('span.sidebar-toggle-btn').hide();
        $('.col-lg-2.layout-side-section').hide();
        
	}
};